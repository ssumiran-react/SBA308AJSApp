This system is demostrating connecting to the API's end-points and displaying the data to the front-end. 
The system pages are developed using front-end tools such as HTML, CSS and JavaScript.
HTML is used as template.
CSS is used to modify the presentation style.
It is using JavaScript to call API.
In JavaScript uses:
axios,
async function,
export, import functions and module
addEventListener,
and etc.
